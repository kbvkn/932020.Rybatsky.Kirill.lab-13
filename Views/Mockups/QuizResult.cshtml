@model Backend3.Models.QuizResultModel
@{
    ViewBag.Title = "Hello Quiz";
}

<h2 style="font-family: Helvetica">Quiz results</h2>
<h4>Right answers @Model.CorrectAnswerCount out @Model.Count</h4>
<ol>
    @foreach (var question in Model.Questions)
    {
        <li>@question.ToString() @question.UserAnswer</li>
    }
</ol>